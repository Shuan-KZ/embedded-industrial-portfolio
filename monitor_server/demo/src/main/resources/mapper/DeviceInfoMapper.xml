<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.DeviceInfoMapper">

    <resultMap id="deviceInfoMap" type="com.example.demo.entity.DeviceInfo">
        <id column="id" property="id"/>
        <result column="device_name" property="deviceName"/>
        <result column="device_code" property="deviceCode"/>
        <result column="device_type" property="deviceType"/>
        <result column="status" property="status"/>
        <result column="location" property="location"/>
        <result column="ip_address" property="ipAddress"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="findAll" resultMap="deviceInfoMap">
        SELECT * FROM device_info ORDER BY id DESC
    </select>

    <select id="findById" parameterType="long" resultMap="deviceInfoMap">
        SELECT * FROM device_info WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.demo.entity.DeviceInfo"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO device_info (device_name, device_code, device_type, status, location, ip_address, create_time, update_time)
        VALUES (#{deviceName}, #{deviceCode}, #{deviceType}, #{status}, #{location}, #{ipAddress}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.example.demo.entity.DeviceInfo">
        UPDATE device_info
        SET device_name = #{deviceName},
            device_code = #{deviceCode},
            device_type = #{deviceType},
            status = #{status},
            location = #{location},
            ip_address = #{ipAddress},
            update_time = NOW()
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM device_info WHERE id = #{id}
    </delete>

</mapper>
